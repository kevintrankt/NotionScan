name = "notionscan-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# Optional: lock the proxy to a single origin (your deployed site) so nobody else
# can route their traffic through your Worker. Uncomment and set your URL.
# [vars]
# ALLOWED_ORIGIN = "https://YOUR_GITHUB_USERNAME.github.io"
